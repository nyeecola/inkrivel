json.extract! character, :id, :name, :hit_points, :damage, :attack_speed, :speed, :model_file, :texture_file, :hitbox_radius, :created_at, :updated_at
json.url character_url(character, format: :json)
